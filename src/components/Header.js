import React from 'react';
import {Text, View, StyleSheet} from 'react-native';
const styles = StyleSheet.create({});

const Header = () => {
  return (
    <View>
      <Text>Administrador de Costos</Text>
    </View>
  );
};

export default Header;
